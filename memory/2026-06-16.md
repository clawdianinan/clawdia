
## IIH Booking Agent Automation Setup - Durable State (2026-06-16)

- Context: User is setting up an IIH facility booking automation around `facilitybookings@iih.ng`, `events@iih.ng`, Zoho Books, Zoho CRM, Zoho Calendar, and Zoho Mail.
- Core workflow goal: Agent monitors booking emails, distinguishes new booking requests from thread replies, sends/handles invoices, waits for customer payment proof, matches proof/payment against invoice amount, then confirms the event on the calendar using `events@iih.ng`.
- Outbound rule: All responses sent from `facilitybookings@iih.ng` must copy `events@iih.ng`.
- Implemented in workspace during prior turn:
  - `scripts/iih_booking_agent.py` polls `facilitybookings`, classifies messages as new booking requests vs thread replies, queues next actions, tracks invoice metadata, detects payment-proof replies, and marks local threads confirmed once proof/payment matches.
  - `scripts/iih_booking_connectors.py` includes live connector steps for calendar confirmation, invoice email sending, and payment reminders.
  - OpenClaw cron job created for booking agent polling every 10 minutes: `d5ff536a-db19-4d9f-a4d2-9ef84d18eb72`.
- Commits created:
  - `98b0e34e86 config: complete IIH booking agent workflow (thread/payment automation)`
  - `0d6bdb23f2 config: allow IIH booking invoice sends and reminders`
- Secrets/status already saved in Keychain or local secure storage without exposing values:
  - `ZOHO_CLIENT_ID`
  - `ZOHO_CLIENT_SECRET`
  - `ZOHO_REFRESH_TOKEN`
  - `ZOHO_APP_PASSWORD`
  - `ZOHO_CALENDAR_UID`
  - `ZOHO_BOOKS_ORG_ID`
- Zoho access status before compaction:
  - Zoho Books org discovered and saved: `Ilorin Innovation Hub Ltd/Gte`.
  - Zoho Books contacts/invoices APIs tested OK.
  - Zoho Calendar tested OK.
  - Zoho Mail API tested OK.
  - CRM was previously blocked because OAuth tokens lacked CRM scopes even after attempts; likely cause was `facilitybookings@iih.ng` not yet enabled as CRM user.
- Latest user update before memory flush: user said `facilitybookings@iih.ng` has been added to Zoho CRM and invitation accepted, and a new OAuth code is ready. Next operational step is to collect the new code through hidden/local prompt, exchange it, save the new refresh token, and retest CRM contacts/search access. Do not ask user to paste secrets in visible chat.
- Correct OAuth intent for retry: include Books, Calendar, Mail, and broad CRM scopes, especially CRM modules/settings/users/org scopes needed for contact/search sync.

## IIH Booking Agent Automation Setup - Durable State (2026-06-16)

- Context: User is setting up an IIH facility booking automation around `facilitybookings@iih.ng`, `events@iih.ng`, Zoho Books, Zoho CRM, Zoho Calendar, and Zoho Mail.
- Core workflow goal: Agent monitors booking emails, distinguishes new booking requests from thread replies, sends/handles invoices, waits for customer payment proof, matches proof/payment against invoice amount, then confirms the event on the calendar using `events@iih.ng`.
- Outbound rule: All responses sent from `facilitybookings@iih.ng` must copy `events@iih.ng`.
- Implemented in workspace during prior turn:
  - `scripts/iih_booking_agent.py` polls `facilitybookings`, classifies messages as new booking requests vs thread replies, queues next actions, tracks invoice metadata, detects payment-proof replies, and marks local threads confirmed once proof/payment matches.
  - `scripts/iih_booking_connectors.py` includes live connector steps for calendar confirmation, invoice email sending, and payment reminders.
  - OpenClaw cron job created for booking agent polling every 10 minutes: `d5ff536a-db19-4d9f-a4d2-9ef84d18eb72`.
- Commits created:
  - `98b0e34e86 config: complete IIH booking agent workflow (thread/payment automation)`
  - `0d6bdb23f2 config: allow IIH booking invoice sends and reminders`
- Secrets/status already saved in Keychain or local secure storage without exposing values:
  - `ZOHO_CLIENT_ID`
  - `ZOHO_CLIENT_SECRET`
  - `ZOHO_REFRESH_TOKEN`
  - `ZOHO_APP_PASSWORD`
  - `ZOHO_CALENDAR_UID`
  - `ZOHO_BOOKS_ORG_ID`
- Zoho access status before compaction:
  - Zoho Books org discovered and saved: `Ilorin Innovation Hub Ltd/Gte`.
  - Zoho Books contacts/invoices APIs tested OK.
  - Zoho Calendar tested OK.
  - Zoho Mail API tested OK.
  - CRM was previously blocked because OAuth tokens lacked CRM scopes even after attempts; likely cause was `facilitybookings@iih.ng` not yet enabled as CRM user.
- Latest user update before memory flush: user said `facilitybookings@iih.ng` has been added to Zoho CRM and invitation accepted, and a new OAuth code is ready. Next operational step is to collect the new code through hidden/local prompt, exchange it, save the new refresh token, and retest CRM contacts/search access. Do not ask user to paste secrets in visible chat.
- Correct OAuth intent for retry: include Books, Calendar, Mail, and broad CRM scopes, especially CRM modules/settings/users/org scopes needed for contact/search sync.

## Session Continuation - After Compaction Flush (2026-06-16 ~12:57-15:00)

### CRM OAuth Completed
- CRM OAuth was finally granted and confirmed working after `facilitybookings@iih.ng` was added as CRM user.
- Full Zoho stack now connected: Mailbox, Books, CRM, Calendar, Mail API.
- Booking connector doctor validated all required secrets saved.
- Commit: `c0ffb5afb4 docs: mark IIH booking CRM OAuth active`

### Aisha Agent Setup Completed
- Aisha registered as named agent in OpenClaw: `openclaw agents list` shows `aisha` (📅 Aisha).
- Full agent directory created: `~/.openclaw/agents/aisha/agent/` with IDENTITY.md, TOOLS.md, TASKS.md, HEARTBEAT.md, SOUL.md, MEMORY.md.
- Main routing docs and AGENTS.md updated to include Aisha as exclusive handler for `facilitybookings@iih.ng`.
- Commit: `749a6a2003 config: set Aisha as booking mailbox owner`

### Aisha Signature + Invoice Rule Changes
- Email signature: `Warm regards,\nAisha\nIIH Facility Booking Agent` (after current Ministry thread).
- Invoice rule: "An invoice is always required for facility bookings" — never ask "if an invoice is required".
- Commit: `0758883daa config: set Aisha signature for future booking emails`
- Commit: `0400afaf05 config: require invoices for all booking requests`

### Payment Confirmation Gate
- Payment proof from customer must be confirmed with `finance@iih.ng` via email before final calendar confirmation.
- Commit: `ff1c815df2 config: add booking form intake and finance confirmation gate`

### First Live Booking Thread — Ministry of Health / Global Fund Visit (June 17, 2026)
- Original request from Oluwatosin Fakayode (tosintolufakayode@gmail.com) and Dr. Amina M. Ahmed El-Imam.
- Aisha responded from `facilitybookings@iih.ng` requesting missing details: time, hall preference, seating, AV, catering, billing details.
- Follow-up sent with booking form link.
- Customer replied at 2:31pm with event details: 9am-1pm, theatre seating, 100+ capacity, projector/AV, security, IIH tea break/lunch requested, wants to inspect hub today.
- Booking state: `Pending Availability`
- Next action: Check `events@iih.ng` calendar availability and prepare invoice path.
- Note: The first response (1:08pm) used old Clawdia signature + "if invoice required" wording before rules were updated.

### Mailbox Monitoring Mechanism
- macOS LaunchAgent active: `ng.iih.booking-agent`
- Runs every 10 minutes polling `facilitybookings@iih.ng` INBOX.
- Command: `python3 scripts/iih_booking_agent.py poll --limit 25 --pretty`
- Logs: `logs/iih-booking/booking-agent.out.log` and `.err.log`
- Verified working — last poll exited 0.
- OpenClaw cron wrapper disabled (timeout issues at isolated agent layer); direct LaunchAgent is the reliable monitor.

### Minor Fix
- Poll classifier fixed to ignore outbound `facilitybookings@iih.ng` responses so agent doesn't mistake own outbound emails for customer replies.
- Commit: `5171e6a554 fix: ignore outbound booking responses in poll classifier`
