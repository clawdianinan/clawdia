# Session: 2026-06-02 04:15 GMT+1

- **Session Key**: agent:main:main
- **Source**: webchat

## Conversation Summary

user: [Tue 2026-06-02 04:15 GMT+1] now my local web openclaw isnt working, i need it to work
assistant: Investigated local OpenClaw web outage using prior memory and runtime checks.

### Findings

- Prior matching incident existed in [memory/2026-06-01-0823.md](/Users/clawdia/.openclaw/workspace/memory/2026-06-01-0823.md): local web broke when `gateway.bind` was switched to `tailnet`.
- Current runtime matched the same pattern:
  - `~/.openclaw/openclaw.json` had `"bind": "tailnet"`
  - `curl http://127.0.0.1:18789/` failed
  - `curl http://100.75.58.42:18789/` returned `200 OK`
  - `lsof` showed the gateway listening on `100.75.58.42:18789` only

### Action Taken

- Created mandatory config backup:
  - `/Users/clawdia/.openclaw/openclaw.json.backup-20260602-041642`
- Changed `gateway.bind` from `tailnet` to `loopback` in:
  - `/Users/clawdia/.openclaw/openclaw.json`
- Restarted the gateway with `openclaw gateway stop` and `openclaw gateway start`

### Verification

- `curl http://127.0.0.1:18789/` returned `HTTP/1.1 200 OK`
- `lsof -iTCP:18789 -sTCP:LISTEN` showed:
  - `127.0.0.1:18789`
  - `[::1]:18789`

### Notes

- Root cause repeated from the previous day: `gateway.bind = "tailnet"` breaks localhost-dependent web access.
- If dual local + remote access is needed later, keep `bind = "loopback"` and expose remote access with Tailscale Serve or an SSH tunnel instead of rebinding the gateway.
